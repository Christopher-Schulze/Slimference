# Slimference Historical Spec

The old v1 specification has been retired.

Use `spec+.md` for the current technical target. Layer 2, MiniMax,
summarization, OCRL, and context-ledger insertion are no longer product paths and
must not be implemented from the historical v1 text.
