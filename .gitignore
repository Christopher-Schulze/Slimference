# Local secrets (never commit)
.env.local
.env.*.local
.secrets/

# Local agent and project-history notes.
# Keep these files on disk for local re-entry, but do not publish them.
docs/todo.md
docs/todo/

# Build artifacts
/benchmarks
/ci
/slimference
/tokenproxy
/tokenproxy.test
/tokenproxy_test
/utils

# Coverage and test output files
*.out
*.test

# IDE
.DS_Store
.idea/
.vscode/
research/
repos/

# Stray tilde directories created by test runs that encountered a literal "~"
# in a path before expandHome was fixed. Real HOME subdirs must never live
# inside the repo tree.
~/
**/~/
/dist/
/release
rtk/
